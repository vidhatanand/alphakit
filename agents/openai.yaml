interface:
  display_name: "Alphakit"
  short_description: "Generate, export, and verify PNG/JPEG/WebP"
  default_prompt: "Use $alphakit to create this image and ask me for PNG, JPEG, or WebP if I did not specify a format."
