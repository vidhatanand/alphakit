interface:
  display_name: "Alphakit"
  short_description: "Generate, export, and verify PNG/JPEG/WebP"
  default_prompt: "Use $alphakit to create this image as a true transparent PNG unless I explicitly request JPEG, WebP, or an opaque output."
