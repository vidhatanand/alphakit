interface:
  display_name: "Image Format and Transparency"
  short_description: "Generate, export, and verify PNG/JPEG/WebP"
  default_prompt: "Use $generate-transparent-image to create this image and ask me for PNG, JPEG, or WebP if I did not specify a format."
